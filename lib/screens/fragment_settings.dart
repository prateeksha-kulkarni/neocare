import 'package:flutter/material.dart';

class FragmentSettings extends StatelessWidget {
  const FragmentSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Settings Page"));
  }
}
