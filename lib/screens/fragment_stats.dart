import 'package:flutter/material.dart';

class FragmentStats extends StatelessWidget {
  const FragmentStats({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Stats Page"));
  }
}
